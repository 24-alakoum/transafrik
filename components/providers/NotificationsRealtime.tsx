'use client'

/**
 * Polling des notifications — remplace Supabase Realtime (feature payante).
 * React Query gère déjà le refetchInterval dans useNotifications().
 * Ce composant ne fait rien mais reste en place pour ne pas casser le layout.
 */
export function NotificationsRealtime() {
  return null
}
